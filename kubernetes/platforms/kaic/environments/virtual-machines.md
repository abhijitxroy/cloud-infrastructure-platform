# KAIC Virtual Machine Environment

## Overview

Virtual Machine (VM) creation is one of the environment types supported by KAIC.

The recorded session identifies VM creation as one of the existing `kaic create` capabilities alongside containers and OpenShift environments.

```text
kaic create
    |
    +-- container
    +-- VM
    +-- OCP
