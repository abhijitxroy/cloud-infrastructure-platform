# KAIC Container Environment

## Overview

Container creation is one of the environment types supported by KAIC.

The recorded session presents containers as one of the existing `kaic create` capabilities alongside virtual machines and OpenShift environments.

```text
kaic create
    |
    +-- container
    +-- VM
    +-- OCP
